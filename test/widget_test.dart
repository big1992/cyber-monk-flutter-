// Game smoke test — verifies the project compiles correctly.
// The default Flutter counter test is not applicable to a Flame game.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('CyberMonk game project compiles', () {
    expect(true, isTrue);
  });
}
